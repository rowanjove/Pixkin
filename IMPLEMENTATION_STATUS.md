# Pixkin v2 实施状态

更新时间：2026-09-13

## 已完成

### M0：规范冻结

- 已完成 `PIXKIN_DESIGN_SPEC_V2.md`。
- 已确认内置角色为山山、凛凛、Pip。
- 已确认椰子只作为独立 ZIP。

### M1：v2 运行时与角色包

- v2 Schema 与质量等级校验。
- 独立帧、动作条和图集统一为帧序列。
- v2 图片解码、像素数量和图集区域校验。
- 结构化人设生成运行提示词。
- v1 包兼容与旧式单帧动效回退。
- 动作优先级、可打断规则和上下文恢复。
- 每角色环境动作权重、冷却和速度配置。
- AI 倾听、思考、说话、工具、成功和失败状态接入。

### M2：四向贴边系统

- 左、右、上、下四向状态。
- 左、右、上默认开启，底边默认关闭。
- 进入、停留、悬停和退出状态。
- 悬停多探出、单击退出后打开聊天。
- 重要提醒临时离边并返回原位置。
- 设置页四边开关、悬停开关和提醒后返回开关。

### M3：首批角色视觉与动画

- 山山、凛凛、Pip、椰子均完成统一 Pixkin Q 版身份基准。
- 每个角色均完成 9 行动作、`192 × 208` 单格、8×9 透明图集。
- 山山、凛凛、椰子因单侧配件或非对称服装独立生成左跑。
- Pip 经人工确认对称后，由右跑安全镜像得到左跑。
- 四个角色均生成接触表、逐状态 MP4、结构检查和图集校验。
- 所有最终图集均为 `1536 × 1872` RGBA WebP，自动 QA 零错误、零警告。
- 人工接触表检查确认角色身份、脸、配色和标志配件一致。

### M4：完整级角色包与发行接入

- 新增 `scripts/build_v2_character_packs.py`，可重复生成 v2 完整级角色包。
- 山山、凛凛、Pip 均提供 44 个标准化状态映射并随发行版内置。
- `main.py`、两份 PyInstaller spec 和预览工具已接入 `pip.zip`。
- 椰子按相同完整级标准生成，但仅输出 `yeye.zip` / `椰子.zip`，不会自动安装。
- 四个 ZIP 均通过 v2 schema、完整级状态、图集尺寸和资源安全校验。

### 工程与发布基线

- 已初始化有效 Git 仓库，默认分支为 `main`。
- 图片、视频、图集和角色 ZIP 已配置 Git LFS，避免二进制历史拖垮仓库。
- 新增 Windows CI：版本元数据检查、源码编译、Qt offscreen 全量测试和
  PyInstaller 文件夹版构建冒烟。
- 新增标签/手动触发的 Windows 发行工作流，发布标签必须与应用版本一致。
- `core/version.py` 已成为运行时、设置页、PyInstaller 和发布文件名的统一版本来源。
- Windows `version_info.txt` 由脚本生成并在 CI 中检查是否过期。
- 文件夹版和便携版均已使用统一版本来源完成本机构建验证。

### M7：发布与质量基线（进行中）

- 已新增 `pyproject.toml`，统一配置 pytest、Coverage、Ruff 与 Pyright。
- Ruff 已覆盖源码、脚本和测试，当前零错误。
- Pyright 已覆盖 `core/`，当前零错误、零警告。
- 全量测试已接入分支覆盖率门禁，当前总覆盖率 79.2%，门槛为 70%。
- 已生成带精确版本和 SHA-256 哈希的 Windows `requirements-lock.txt`。
- 已接入 `pip-audit` 和 CycloneDX SBOM；本轮 `pip-audit` 因环境网络无响应未完成，不能据此宣称依赖无已知漏洞。
- CI 与 Release Workflow 已改用锁定依赖和统一质量脚本。
- 本地发行脚本会在构建前执行完整质量门禁，并把 `SBOM.cdx.json` 纳入发布哈希。
- 文件夹版和便携版均已使用隔离用户目录完成自动启动、正常退出和日志验证。
- CI 已增加 Windows Server 2022/2025、100%/200% DPI 自动兼容矩阵；
  负坐标副屏位置恢复和浮层边界已有纯逻辑测试。
- 待完成：配置远程仓库与分支保护，以及 Windows 10/11 实机的
  125%/150% DPI、热插拔、休眠唤醒和任务栏位置验收。

### M8：架构解耦（已完成）

- 新增 Provider 中立的聊天接口、能力声明和健康检查契约。
- OpenAI 兼容 SDK、流式文本和 Tool Call 拼装已从工作线程移入 Provider。
- 系统消息、上下文和工具调用循环已移入 `ChatService`。
- 角色会话、上下文裁剪和聊天历史持久化已移入 `ChatSessionService`；
  `DesktopPetApp` 不再直接裁剪消息或写入聊天数据库。
- `AiWorkerThread` 现在只负责线程生命周期、取消和 Qt 信号转发。
- 保存位置恢复、负坐标副屏判断和聊天浮层定位已下沉为纯布局服务。
- 伙伴工坊已建立 13 个显式阶段，非法跳转和状态历史篡改会被拒绝。
- 失败/取消任务会根据中断前阶段和现有产物回到安全恢复入口。
- 任务清单保留兼容 schema 1，并增加工作流 schema 2；旧任务迁移测试通过。
- 孵化任务增加旧版可忽略的工作流版本与状态历史字段，外层 schema 仍为 1。
- 新增 `CharacterService` 与不可变安装计划，统一 ZIP 预检、已安装冲突、
  内置角色保护、覆盖确认、安装/激活、重命名和删除回退。
- 安装确认前记录归档 SHA-256，真正写入前重新解析并核对摘要与角色 ID，
  防止确认后的角色包被替换。
- 主程序内置包同步、官方包升级、设置页导入、首次导入和伙伴工坊均已接入
  同一个角色服务；UI 不再直接执行 ZIP 安装和目录删除事务。
- 角色包目录、配置字段和 ZIP schema 未改变，现有安装无需迁移。
- 新增 `PetLabService`，审核接受/返工/暂存会返回显式命令并统一原子清单写入。
- 未完成任务的身份审核、核心审核、QA 审核、最终安装或继续生成入口由
  Service 根据阶段和实际产物共同决定；缺失产物时安全回退到恢复生成。
- 任务工具状态、QA 返工顺序与候选列表解释已下沉为无界面数据契约。
- 候选切换集中处理当前精灵复制、审核/QA/包产物失效和横向镜像依赖重置。
- 新增独立的候选版本选择、最终安装确认、动画循环预览和生成诊断展示组件。
- 最终安装确认默认选择“否”；动画预览关闭时由组件统一停止并释放
  `QMovie` 文件句柄。
- 身份/API 表单、孵化模式联动、预算计算和输入规范化已移入
  `PetLabHatchForm` 与不可变 `PetLabHatchInput`。
- `PetLabWindow` 保留原控件名兼容别名，但启动孵化只消费规范化数据契约；
  引用图片列表和任务工具控件同样保留兼容入口。
- 新增 `ReferenceImagePicker`，统一参考图去重、四张上限、顺序和缩略图。
- 新增 `PetLabTaskPanel`，统一未完成任务、健康摘要、诊断入口和重试/候选按钮状态。
- 新增 `PetLabDiagnosticsController`，负责脱敏摘要、Issue 模板、匿名问题包导出与检查。
- 新增 `PetLabReviewDialogController`，统一身份稿、核心动作与自动 QA 审核对话。
- 新增 `PetLabInstallDialogController`，统一最终接触表/归档预览、动画入口和
  安装/覆盖确认。
- `PetLabWindow` 的可执行语句由 967 降至 462，已进入 500 行建议线。
- 新增 Provider 中立的图像接口、能力声明、模型健康检查与统一错误分类。
- OpenAI 兼容图像编辑协议、响应 Base64 解码和 SDK 生命周期已移入
  `OpenAICompatibleImageProvider`。
- `PetGenerationWorker` 支持注入 Image Provider；Fake Provider 契约测试无需
  OpenAI SDK 或网络，其可执行语句由 618 降至 554。
- 新增 Provider 中立的直播接口、能力/健康契约和平台路由；B站与抖音 HTTP
  请求及安全 URL 校验已移入 `core/providers/live/`。
- 新增 `LiveService`，负责开播通知转换、最后可信状态、逐房间指数退避和周期汇总。
- 平台探测失败会保留最后一次可信直播状态并独立增加退避，不会制造错误的下播状态；
  成功探测后清除失败并恢复正常轮询间隔。
- `LiveMonitorThread` 现在只负责并发调度、停止等待和 Qt 信号转发。
- B站与抖音解析已加入脱敏响应样本契约测试。
- 设置页直播间表格、增删、平台能力选项和配置规范化已移入
  `LiveRoomEditor`。
- 新增 `UserProfileEditor` 与不可变 `UserProfileInput`，统一用户名称规范化、
  头像预览、选择和清除；文件持久化仍参与设置窗口的集中保存事务。
- 新增 `CharacterManagerPanel`，统一角色列表、详情、内置角色排序与保护、
  旧别名隐藏、预览和操作状态。
- `SettingsWindow` 保留原控件和方法兼容入口，可执行语句由 692 降至 440，
  已进入 500 行建议线。
- 新增 `ChatMessageView`、独立消息行/欢迎卡、日期标签和安全转录渲染；
  `ChatBubbleWindow` 可执行语句降至 410。
- 新增 `UserProfileService` 和 `ChatExportService`，统一头像验证/复制与
  Markdown/JSON 原子导出，UI 不再直接执行这些文件事务。
- 新增 `PetRenderer`，统一角色包帧缓存、方向贴边图和内置角色绘制；
  `PetWindow` 可执行语句降至 412。
- 角色包安装/覆盖回滚、重命名回滚、删除回退和可信归档比对已从解析管理器
  拆为独立事务组件；`CharacterPackageManager` 类内语句由 613 降至 500。
- 新增 `core/storage/migrations.py`，集中声明配置与聊天数据库当前版本及逐版迁移。
- `config.json` 新增顶层 `schema_version: 1`；无版本配置按 0→1 迁移，
  保留用户字段、补齐默认项并完成旧 DeepSeek 模型名迁移。
- 聊天数据库使用 SQLite `PRAGMA user_version = 1`；旧库升级会保留会话和消息，
  同时补齐索引。
- 当前版本重复加载不会再次写入；高于当前版本的数据会被拒绝覆盖并提示用户。
- SQLite 建表、索引和版本更新处于同一事务，中途失败会回滚 DDL 与版本号。

### M9：安全、隐私与工具权限（已完成）

- 对话和图像 API Key 仅持久化到 Windows Credential Manager。
- 设置中心与伙伴工坊在凭据写入失败时直接阻断，不再回退写入明文配置。
- 聊天 Worker 不再读取 `config.json` 中的旧明文 Key。
- 启动时迁移聊天与图像旧明文 Key；只有凭据写入和配置清理都成功才提交，
  配置清理失败会恢复旧凭据并保留原配置。
- 安全策略测试覆盖成功迁移、失败回滚、设置/工坊阻断和运行时无明文消费。
- 新增 L0–L3 工具权限契约；L0 自动执行，L1 无逐次确认时拒绝，
  L2/L3 默认禁用。
- 工具审计事件记录等级、脱敏参数、授权方式、结果和耗时；密钥和 URL
  凭据不会进入审计。
- L1 外部动作由 Worker 桥接到主线程逐次确认，可按相同工具和参数记住本次运行决定；
  L2/L3 继续默认禁用。
- 审计以 1000 条/1 MiB 上限原子落盘，设置中心支持查看、导出和清除。
- 首次发送展示模型端点与内容范围；历史支持不保存、7 天、30 天和永久，
  并可导出后删除或按角色清理。
- 对话与图像 API Key 均可仅在本次运行内保存，退出时清空。
- 日志与聊天数据库使用统一密钥模式脱敏。
- 官方角色包由内置 SHA-256 清单验证；第三方包安装前展示作者、许可、资源数和指纹。
- `cmd.exe` 已从应用白名单和模型可见工具说明中移除。
- 详细边界见 `docs/M9_SECURITY.md`。

> M10–M12 下方的原始验收数字保留为阶段快照；本次 Capability 2.0.0 的最终门禁与产物证据以文末 v1.5.0 节为准。

### M10：稳定性与可观测性（已完成）

- 结构化、滚动且脱敏的运行日志，以及有固定数量上限的本地崩溃摘要。
- 用户主动导出的匿名诊断包只包含严格白名单文件，并校验大小与 SHA-256。
- Chat Provider 支持无聊天内容的连接/模型健康检查、分类恢复建议和失败后重试。
- 直播房间独立指数退避，失败保留最后可信状态；托盘显示逐房间健康信息。
- 聊天数据库迁移前备份；损坏时可只读导出或确认后备份重建。
- 配置、聊天、角色、伙伴工坊任务和资料支持统一校验备份与启动前原子恢复。
- 建立 Windows 10 文件夹版性能基线和 10% 退化判定，详见
  `docs/M10_STABILITY.md`。
- 验收结果：294 项测试与 32 个子测试通过，分支覆盖率 78.07%；
  文件夹版和便携版 EXE 均完成隔离启动退出冒烟。

### M11：安装、升级与自动更新（已完成）

- 新增固定 AppId、当前用户安装、可选快捷方式/开机启动的 Inno Setup 安装器。
- 覆盖升级复用安装目录，普通安装拒绝降级；卸载可选择保留或删除用户数据。
- 更新清单使用离线 Ed25519 私钥签名，应用只打包公钥。
- 清单、最终 HTTPS URL、文件名、大小和 SHA-256 全部严格验证，拒绝外部命令字段。
- stable/beta 渠道、每天最多一次自动检查、分步下载/安装确认和便携版隔离已接入设置页。
- 安装和回滚前统一备份本地数据；回滚只使用受控目录内哈希验证通过的上一安装器。
- CI 编译安装器；正式 Release 缺少私钥 Secret 或签名更新清单时失败。
- 详细设计、密钥运维和验收证据见 `docs/M11_INSTALL_UPDATE.md`。
- 验收结果：310 项测试与 32 个子测试、77.84% 分支覆盖率；
  三种交付形态构建通过，安装、安装版冒烟和保留数据卸载闭环通过。

### M12：产品能力增强（已完成）

- 长期记忆候选只在本机提取，逐条确认后按本机用户/角色保存；可编辑、停用、
  删除和总关闭，每条回复可展开查看实际注入项。
- 按住说话使用独立 HTTPS 转写端点和独立凭据，录音只驻留内存；麦克风状态持续可见。
- 显示/隐藏、打开聊天、停止生成和静音四类 Windows 全局快捷键支持冲突提示和退出注销。
- 角色包支持不安装检查/预览、SHA-256 指纹、兼容判断、官方目录更新和第三方信任隔离。
- 角色动作权重、冷却和贴边露出可按角色覆盖，并支持即时预览且不修改原包。
- AI 支持快速/均衡/深入预设、延迟/token/费用估算、停止、重试和编辑重发；
  Provider 不支持的流式或 Tool Calls 能力会在网络请求前阻断。
- 直播扩展采用显式启用的 entry-point 协议，内置平台严格验证 HTTPS/主机，
  凭据按平台和房间隔离；提醒支持免打扰、补提醒、重复间隔与分组。
- 长期记忆已纳入统一备份/恢复；旧配置与旧直播房间结构继续兼容。
- 详细设计与验收证据见 `docs/M12_PRODUCT_CAPABILITIES.md`。
- 验收结果：333 项测试与 32 个子测试、总覆盖率 77.63%；
  Ruff/Pyright、隔离源码、PyInstaller 文件夹版与便携版冒烟通过。

### M6：伙伴工坊 v2 可恢复闭环

- 新增原子写入的孵化任务清单。
- 每次孵化会记录阶段、动作任务、尝试次数、产物、失败和取消状态。
- 生成任务清单排除 API Key，仅保存恢复任务所需的请求信息。
- 参考图会复制进独立任务工作区，原文件移动或删除后仍可恢复。
- 身份稿生成后会暂停，用户可以确认、重新生成或留待稍后处理；未确认前不会生成动作。
- `idle`、`talking`、`dragging`、`alerting` 生成后会暂停进行核心动作一致性审核；
  完整孵化只有审核通过后才生成其余动作。
- 新增未完成孵化任务列表，可恢复身份审核、动作生成、失败任务和最终安装。
- 失败或取消的动作可以单独重试，已完成动作不会重复生成。
- 每次生成会保留原始图像响应和规范化后的精灵，返工不会覆盖上一版文件。
- 每个任务记录全部候选、生成模型、质量、提示词和原始文件哈希，并明确记录当前选择。
- 身份审核、核心动作审核、QA 返工和未完成任务工具均可浏览缩略图并切换历史候选。
- 切换动作候选会使相关审核、QA 与旧角色包失效；切换身份候选还会使后续动作失效，
  防止不同身份版本混装。
- 旧任务首次返工前会自动把当前精灵归档为 `legacy-001` 候选。
- 新增伙伴工坊自动 QA：检查图片解码、`192 × 208` 画布、透明轮廓、安全边距、
  基准线漂移、绿边风险、角色尺寸漂移和不同语义动作的精确重复。
- 每次核心审核和最终 QA 都会生成 JSON 报告与动作接触表；阻断错误必须选择动作返工。
- 最终安装或覆盖确认优先展示全动作接触表，而不是只展示 `idle` 缩略图。
- 已确认的关键姿态会围绕 `y = 194` 锚点生成 4–5 帧呼吸、轻摆或弹跳微动作，
  不额外调用图像 API，也不改变角色身份细节。
- 新增帧级 QA：检查帧数、FPS、播放模式、画布、透明轮廓、安全边距、基准线、
  可见运动、帧间尺寸和位置漂移。
- 每个动作生成独立 GIF 预览；最终安装与覆盖确认可打开动作下拉框实时播放。
- 新生成角色包的 `source.type: frames` 已直接引用多帧文件，`dragging` 等持续状态
  使用循环播放，瞬时动作使用单次播放。
- 动画预览关闭时主动释放 `QMovie` 文件句柄，避免 Windows 阻止任务清理或重新生成。
- 上一版停在最终安装确认但没有 QA 报告的任务，恢复时会复用现有图片补做审核与 QA。
- 角色包生成后进入最终预览，明确确认后才安装；拒绝安装会保留任务与 ZIP。
- 审核决定与安装结果写入同一份原子任务清单，API Key 仍不会落盘。
- 伙伴工坊新生成图片已统一为 `192 × 208` RGBA 画布和 `y = 194` 基准线。
- 基础孵化会独立生成基础级所需的 `idle`、`talking`、`dragging` 和
  `alerting`，不再把同一张图片伪装成全部动作。
- 伙伴工坊已直接输出带结构化人设、行为、兼容性和权利声明的 v2 基础级角色包，
  不再生成新的 v1 包。

### v1.5.0：Capability 2.0.0 实施状态（RELEASE CANDIDATE）

公开版本号固定为 `1.5.0`，能力里程碑记为 `2.0.0`，不发布 `1.6/1.7/1.8/2.0` 公开号。

- Runtime：Kernel、ServiceContainer、EventBus、ActionDispatcher 和可测试 StartupCoordinator 已落地。
- Provider：Chat/Image/TTS 使用统一 Registry；ASR、Vision、Embedding、Realtime、Trigger 已预留同一扩展契约；ModelProfile 支持能力 override。
- Speech：VAD 迟滞、SpeechState、AI 打断、ASR 注入边界和 TTS 状态同步已落地，旧按住说话入口保持兼容。
- Perception：窗口、进程、系统空闲、剪贴板和按需截图接入 default-deny 权限；ContextBudget 防止无关内容全量进入 Prompt。
- Memory/Character：SQLite+FTS5 MemoryV2、JSON 幂等迁移、角色状态持久化、Renderer 契约和 Character Card 编解码已落地；角色包接受 v3 runtime sections。
- Plugin/MCP/Tools：进程隔离插件、safe mode、stdio/Streamable HTTP MCP、工具 metadata/超时/执行上下文/脱敏审计已落地。
- Governance：Apache-2.0、架构检查脚本和质量脚本门禁已加入。

详细验收矩阵见 [docs/ROADMAP_1.5_CAPABILITY_2.0.md](docs/ROADMAP_1.5_CAPABILITY_2.0.md)。

当前源码质量证据：质量门禁 `459 passed, 46 subtests passed`，总覆盖率 `74.99%`；Ruff、Pyright（Python 3.11）、架构检查和 compileall 通过。本轮修复后的 onedir 与 portable 已重新构建并通过隔离启动/退出烟测；安装器、`pip-audit`、SBOM 和正式发布目录仍需重新执行后才能恢复为完整发布证据。

未在本环境演练的手工场景：真实麦克风/ASR、云端 TTS、物理多显示器（Windows 10/11 实机差异）以及真实外部 MCP 服务。它们不改变源码和自动化发布物状态，但在面向终端用户发布前仍应按验收矩阵执行。

本轮计划明确不包含购买商业代码签名证书，SmartScreen 零提示不作为验收目标。

## 测试基线

- Python：3.11
- Qt：offscreen 测试模式
- 当前结果：全量测试与覆盖率以最终质量门禁输出为准；门槛为 70%。
- PyInstaller：文件夹版与便携版构建通过
