# Pixkin 后续升级计划

更新日期：2026-07-26

## 1. 计划定位

Pixkin 当前已经完成桌宠运行时、v2 角色包、伙伴工坊三档孵化、AI 对话与工具、
聊天历史、直播监听以及 Windows 双形态发行构建。本计划的目标不是继续无序增加功能，
而是先把项目从“功能完整的 Beta”升级为“可持续维护、可重复发布、可安全扩展的稳定产品”。

本计划明确排除以下事项：

- 不包含购买商业代码签名证书。
- 不把 Windows SmartScreen 零提示作为本轮验收目标。
- 不在 Windows 稳定性基线完成前启动 macOS、Linux 或移动端移植。

本计划采用里程碑门禁。前一里程碑的阻断项未通过时，不进入依赖它的下一阶段发布。
工作量使用人日估算，适用于一名熟悉当前代码库的开发者；并行投入可缩短日历时间，
但不能跳过验收门禁。

## 2. 总体优先级

| 优先级 | 里程碑 | 目标 | 预计工作量 |
| --- | --- | --- | ---: |
| P0 | M7 发布与质量基线 | 建立可信、可重复、可验收的发布流程 | 8–12 人日 |
| P0 | M8 架构解耦 | 降低大文件和 UI/业务耦合带来的回归风险 | 12–18 人日 |
| P0 | M9 安全、隐私与工具权限 | 消除明文密钥回退，建立工具授权和审计 | 8–12 人日 |
| P1 | M10 稳定性与可观测性 | 提升网络、数据库、崩溃和性能问题的可诊断性 | 8–12 人日 |
| P1 | M11 安装、升级与自动更新 | 提供可验证的安装、覆盖升级、卸载和回滚能力 | 8–12 人日 |
| P2 | M12 产品能力增强 | 在稳定基础上增加记忆、语音、快捷操作和生态能力 | 按主题拆分 |

建议依次完成 M7、M8、M9；M10 可以在 M8 后半段并行推进；M11 必须复用 M7
建立的发布产物和质量门禁；M12 只能在 M7–M10 的阻断项全部关闭后进入主线。

### M7 当前执行状态

状态：`completed`

已完成：

- pytest、Coverage、Ruff、Pyright 的统一配置和本地质量脚本。
- 70% 分支覆盖率门禁；当前基线为 79.2%。
- 带精确版本和哈希的 Windows 依赖锁文件。
- 依赖漏洞审计和 CycloneDX SBOM。
- CI、Release Workflow 与本地发布脚本的质量门禁接入。
- 文件夹版和便携版 EXE 的隔离启动、正常退出和日志验证。
- Windows Server 2022/2025、100%/200% DPI 自动兼容矩阵，以及负坐标副屏布局测试。

下一批：

- 远程仓库、分支保护和 Git LFS 远程完整性检查。
- Windows 10/11 实机的多显示器、125%/150% DPI、热插拔、休眠唤醒和任务栏位置验收。

## 3. M7：发布与质量基线

### 3.1 目标

让每个候选提交都能自动回答四个问题：代码是否合格、测试是否充分、发行物是否可启动、
发布说明是否与实际实现一致。

### 3.2 工作项

1. 建立远程仓库与分支保护
   - 配置 GitHub 远程仓库并推送完整 Git LFS 对象。
   - 保护 `main`，要求 CI 全部通过后才能合并。
   - 启用 GitHub Actions 缓存、构建产物留存和失败日志上传。
   - 标签发布必须与 `core/version.py` 中版本完全一致。

2. 建立静态质量门禁
   - 增加 `pyproject.toml`，集中配置 Ruff、Pyright 和 pytest。
   - Ruff 覆盖源码、脚本和测试，阻断未使用导入、明显错误和不一致格式。
   - Pyright 先覆盖 `core/`，再逐步覆盖 `ui/`；不得以全局忽略代替修复。
   - 增加 `pytest-cov`，核心模块行覆盖率目标不低于 80%，项目总覆盖率不低于 70%。
   - 覆盖率下降超过 2 个百分点时阻断合并。

3. 固定依赖和供应链检查
   - 保留人类维护的顶层依赖文件，生成带精确版本和哈希的 Windows 锁定文件。
   - CI 使用锁定依赖，不再直接解析开放式最低版本。
   - 增加 `pip-audit` 或等价漏洞扫描。
   - 每个正式版本生成依赖清单和 SBOM，随构建记录保存。

4. 自动同步项目事实
   - 测试数量、版本号和发行文件名不得手工重复维护。
   - 增加文档一致性测试，检查 README、版本资源、发布脚本和发布清单。
   - `IMPLEMENTATION_STATUS.md` 只记录已经完成且有提交/测试证据的能力。
   - 本文件作为唯一后续路线来源，其他文档只引用，不复制路线。

5. 扩展发行物自动验收
   - CI 同时构建文件夹版和便携版。
   - 在隔离的 `%LOCALAPPDATA%` 下启动 EXE，等待“启动完成”日志，再请求正常退出。
   - 验证托盘进程退出后没有残留 Pixkin 子进程。
   - 自动导入随发行版提供的椰子角色包，并检查预览和角色切换。
   - 验证发行 ZIP、EXE、角色包与 `SHA256SUMS.txt` 一致。

6. 建立真实 Windows 测试矩阵
   - 至少覆盖 Windows 10 和 Windows 11。
   - 覆盖单屏、双屏，100%、125%、150%、200% DPI。
   - 覆盖主显示器在左/右、负坐标副屏、任务栏位置变化。
   - 覆盖休眠唤醒、显示器热插拔、网络断开恢复和系统主题切换。
   - 首轮记录基线，后续发布只需执行阻断场景与发生变化的扩展场景。

### 3.3 交付物

- `pyproject.toml` 与锁定依赖文件。
- 扩展后的 CI、Release Workflow 和发行物冒烟脚本。
- 自动生成的覆盖率、漏洞扫描和 SBOM 产物。
- 文档一致性检查脚本。
- 填写完整的发布验收记录。

### 3.4 完成门禁

- 全量测试、Ruff、Pyright、覆盖率、依赖审计全部通过。
- 文件夹版和便携版在 CI 中完成启动、退出和无残留进程验证。
- Windows/DPI/多屏矩阵不存在 P0、P1 问题。
- 发布清单所有必选项有日期、候选提交和验收人记录。
- README、实施状态和实际测试数量不存在冲突。

## 4. M8：架构解耦

### M8 当前执行状态

状态：`completed`

当前已完成：

- 建立 Chat Provider 契约、能力声明和健康检查接口。
- 将 OpenAI 兼容协议、流式文本与 Tool Call 拼装移入 Provider。
- 将系统消息、上下文和工具循环移入 `ChatService`。
- 将角色会话、上下文裁剪和聊天历史持久化移入 `ChatSessionService`。
- `AiWorkerThread` 只保留线程生命周期、取消和 Qt 信号转发。
- 将桌宠位置恢复和聊天浮层定位下沉为可独立测试的布局服务。
- 伙伴工坊已建立 13 阶段状态机、转换历史、失败/取消恢复路径和旧任务迁移。
- 新增 `CharacterService`，统一角色包预检、冲突判定、安装确认、激活、
  重命名与删除回退。
- 安装计划记录 ZIP SHA-256，并在落盘前重新校验，拒绝确认后被替换的归档。
- 内置角色覆盖被建模为受保护冲突；只有发行内置包同步可显式进入可信覆盖路径。
- 主程序、设置页、首次导入和伙伴工坊均已通过 Service 调用角色包事务。
- 新增 `PetLabService`，统一审核决定落盘、未完成任务恢复路由、任务工具状态、
  QA 返工候选与历史候选切换。
- 候选切换会集中失效相关审核、QA、动画和旧角色包产物，并重置横向镜像依赖。
- 候选版本选择、最终安装确认、动画循环预览和生成诊断展示已拆到
  `ui/pet_lab_components.py`，安装确认默认选择“否”。
- 动画预览组件在关闭时统一停止并释放 `QMovie`，避免 Windows 文件句柄残留。
- 身份/API 表单、模式联动、预算计算和输入规范化已拆为 `PetLabHatchForm`
  与不可变 `PetLabHatchInput` 契约；API Key 不参与数据对象的调试表示。
- 引用图片选择已拆为 `ReferenceImagePicker`，统一去重、四张上限、顺序和缩略图。
- 未完成任务选择、健康摘要和工具按钮状态已拆为 `PetLabTaskPanel`；
  诊断复制、匿名问题包导出/检查已拆为 `PetLabDiagnosticsController`。
- 身份稿/动作 QA 审核已拆为 `PetLabReviewDialogController`，最终包预览、
  动画入口与安装/覆盖确认已拆为 `PetLabInstallDialogController`。
- `PetLabWindow` 的可执行语句已从 967 降至 462，进入 500 行建议线；
  业务分支可在无 Qt 环境测试。
- 建立 Image Provider 契约、能力声明、模型健康检查和统一错误分类；
  OpenAI 兼容图像编辑协议、响应解码与 SDK 生命周期已移出生成工作线程。
- `PetGenerationWorker` 支持注入 Provider，契约测试可完全使用 Fake Provider，
  无需网络或 OpenAI SDK；其可执行语句已从 618 降至 554。
- 建立 Live Provider 契约、能力/健康接口和平台路由，B站与抖音 HTTP 协议已
  从监控线程移入 `core/providers/live/`。
- 新增 `LiveService`，统一开播通知转换、最后可信状态、逐房间指数退避和周期汇总；
  检测失败不再把上一次可信“直播中”状态错误覆盖为“已下播”。
- `LiveMonitorThread` 只保留并发探测、停止等待和 Qt 信号转发职责。
- B站与抖音使用脱敏响应样本进行解析契约测试，平台响应结构变化可在 CI 中暴露。
- 设置页直播间表格已拆为 `LiveRoomEditor`，平台选项直接来自 Live Provider
  能力声明，空白房间过滤与字段规范化由组件统一处理。
- 设置页用户资料编辑已拆为 `UserProfileEditor` 与不可变 `UserProfileInput`，
  统一名称规范化、头像预览、选择和清除；头像文件事务由 `UserProfileService`
  集中提交。
- 设置页角色列表和详情已拆为 `CharacterManagerPanel`，统一内置角色排序与保护、
  旧别名隐藏、预览和操作状态。
- `SettingsWindow` 保留原控件和方法兼容入口，可执行语句已从 692 降至 440，
  进入 500 行建议线。
- 聊天消息输入规范化、头像、普通/状态/错误/直播消息行、欢迎卡、日期标签和
  安全转录已拆到 `ui/chat_message_components.py`；`ChatBubbleWindow`
  可执行语句降至 410。
- 用户头像验证/复制与聊天历史 Markdown/JSON 原子导出已分别下沉到
  `UserProfileService` 和 `ChatExportService`，UI 不再直接执行这两类文件事务。
- 角色包帧缓存、方向贴边图和内置角色绘制已移入 `PetRenderer`；
  `PetWindow` 可执行语句降至 412。
- 角色包安装/覆盖回滚、重命名回滚、删除回退和归档比对已拆为独立事务组件；
  `CharacterPackageManager` 类内可执行语句由 613 降至 500。
- 新增统一存储迁移模块；`config.json` 使用顶层 `schema_version: 1`，
  聊天 SQLite 使用 `PRAGMA user_version = 1`。
- 无版本旧配置和旧聊天库会原地升级并保留用户值/聊天消息，重复启动不重复迁移。
- 未来版本数据会被拒绝覆盖并显示明确提示；SQLite DDL 迁移在单事务中失败回滚。
- 设计与迁移边界见 `docs/M8_ARCHITECTURE.md`。

完成审计：

- Chat、Image、Live Provider 契约及无网络 Fake/录制响应测试通过。
- 配置、聊天数据库和旧工坊任务迁移/重复加载/失败回滚测试通过。
- UI 不直接依赖 OpenAI SDK、HTTP、SQLite 或角色/资料文件事务。
- `PetLabWindow`、`SettingsWindow`、`ChatBubbleWindow`、`PetWindow` 及
  `CharacterPackageManager` 均不超过 500 条建议线。
- 全量 231 项测试与 20 个子测试、78.9% 分支覆盖率、打包构建和隔离冒烟通过。

### 4.1 目标

把 UI、网络、流程状态、持久化和发行逻辑分开，使新增模型、工具、角色生成步骤或界面时，
不需要同时修改多个千行级类。

### 4.2 目标结构

```text
core/
├─ providers/
│  ├─ chat/
│  │  ├─ base.py
│  │  └─ openai_compatible.py
│  ├─ image/
│  │  ├─ base.py
│  │  └─ openai_compatible.py
│  └─ live/
├─ services/
│  ├─ chat_service.py
│  ├─ character_service.py
│  ├─ live_service.py
│  ├─ tool_permission_service.py
│  └─ update_service.py
├─ generation/
│  ├─ workflow.py
│  ├─ state_machine.py
│  ├─ packaging.py
│  └─ review.py
└─ storage/
   ├─ chat_store.py
   ├─ generation_store.py
   └─ migrations.py
```

目录名称可以在实施时调整，但层级责任必须保持。

### 4.3 工作项

1. 对话解耦
   - 定义聊天 Provider 接口：流式文本、Tool Calls、取消、健康检查、模型能力。
   - `AiWorkerThread` 只负责线程生命周期，不再拼装业务消息或决定工具循环策略。
   - 会话裁剪、系统提示、工具结果和持久化由 `ChatService` 统一处理。

2. 伙伴工坊状态机
   - 将身份生成、身份审核、核心动作、核心审核、扩展动作、QA、最终审核、安装建模为显式状态。
   - 每个状态声明允许的输入、输出、重试和恢复路径。
   - UI 仅渲染状态和发送命令，不直接控制工作流细节。

3. 角色包服务
   - 将 ZIP 安全检查、元数据解析、安装事务和角色激活拆为独立组件。
   - 保持现有安全限制和测试，不在重构中放宽边界。
   - 当前已完成应用服务边界；底层安全解析和原子目录替换继续由
     `CharacterPackageManager` 承担，后续可在不影响 UI 的前提下继续细拆。

4. UI 瘦身
   - `PetLabWindow`、`SettingsWindow`、`ChatBubbleWindow` 拆分为页面/组件和控制器。
   - 单个业务模块建议不超过 500 行；超过时必须说明职责不可拆分的原因。
   - UI 测试主要验证交互，业务判断下沉到无界面单元测试。

5. 兼容迁移
   - 保持现有配置、角色目录、聊天数据库和未完成孵化任务可读。
   - 对每个存储格式增加显式 schema 版本与迁移测试。
   - 每一步重构独立提交，不进行一次性大改。

### 4.4 完成门禁

- 现有测试覆盖的行为能力不丢失，新接口均有契约测试。
- 伙伴工坊可以从每个等待/失败阶段恢复。
- UI 类不再直接访问 OpenAI、requests、SQLite 或文件事务。
- 旧配置、旧角色包、旧聊天历史和旧孵化任务迁移测试通过。
- 千行级业务类完成拆分，或有明确的设计例外记录。

## 5. M9：安全、隐私与工具权限

### M9 当前执行状态

状态：`completed`

当前已完成：

- 对话与图像 API Key 只持久化到 Windows Credential Manager。
- 设置中心和伙伴工坊在凭据保存失败时阻断，不再回退写入 `config.json`。
- 聊天 Worker 已移除配置明文回退读取。
- 启动迁移同时覆盖聊天和图像旧明文 Key；配置清理失败会回滚凭据并保留旧值。
- 安全策略测试覆盖成功迁移、失败回滚、UI 阻断和运行时不消费明文。
- 新增 L0–L3 工具权限契约：L0 自动执行，L1 无逐次确认时默认拒绝，
  L2/L3 默认禁用。
- L1 工具通过 Worker→Qt 主线程逐次确认，可按精确参数记住本次运行决定；
  切换会话和退出会安全取消等待。
- 工具调用生成带会话编号的脱敏审计事件，并以 1000 条/1 MiB 上限原子持久化；
  用户可以查看、导出和清除。
- `cmd.exe` 已从默认应用白名单和模型可见描述中移除。
- 首次模型调用展示发送端点与范围；聊天历史支持不保存、7 天、30 天和永久，
  并支持导出后删除、按角色清理。
- 对话与图像 API Key 均支持仅本次运行使用，退出时从内存清空。
- 官方角色包使用内置 SHA-256 清单；第三方包安装前展示作者、许可、资源数和指纹。
- 日志和聊天数据库使用统一密钥模式脱敏。
- 设计与证据见 `docs/M9_SECURITY.md`。

完成证据：全量质量门禁、PyInstaller 目录版构建和隔离用户目录 EXE 冒烟通过。

### 5.1 目标

默认情况下不把密钥明文落盘；任何会影响用户界面之外状态的模型工具，都经过可理解、
可撤销、可审计的授权。

### 5.2 工作项

1. 密钥存储
   - Windows Credential Manager 保存失败时，不再回退写入明文 `config.json`。
   - 用户可以选择“仅本次会话使用”，退出后从内存清除。
   - 已存在的明文 Key 在成功迁移后原子删除；迁移失败时保持旧值并明确告警。
   - 日志、诊断包、异常文本和剪贴板导出统一执行密钥模式脱敏。

2. 聊天隐私
   - 首次启用模型时明确说明：发送目标、发送内容范围和本地保存位置。
   - 提供历史保留策略：不保存、7 天、30 天、永久。
   - 增加“导出后删除”和“一键清除角色全部数据”。
   - 评估使用 Windows DPAPI 对消息内容做应用层加密；先完成迁移与恢复原型，再决定默认开启范围。

3. 工具权限模型
   - L0 只读：时间、算式、基础系统信息，可自动执行。
   - L1 外部动作：打开网页、打开应用，默认逐次确认，可选择本次会话记住。
   - L2 状态修改：未来的文件写入、删除、消息发送、系统设置，必须逐次确认。
   - L3 高风险：命令执行、安装程序、凭据访问，默认禁用，不能仅由模型自行开启。
   - 从默认白名单中移除 `cmd.exe`，除非未来有独立、受约束的命令工具设计。

4. 工具审计
   - 记录时间、会话、工具名、参数摘要、授权方式、结果状态和耗时。
   - 不记录 API Key、完整聊天内容或敏感本地路径。
   - 用户可以查看、导出和清除工具审计记录。

5. 角色包信任
   - 保留现有 ZIP 路径、大小、像素、符号链接和压缩比检查。
   - 为官方角色包增加内置 SHA-256 清单。
   - 第三方角色包展示作者、权限声明、资源数量和包指纹后再安装。

### 5.3 完成门禁

- 自动化测试证明任何密钥都不会写入配置、日志、诊断包或聊天数据库。
- L1 以上工具未经授权不能执行。
- 所有工具调用均能在审计页面找到对应结果。
- 旧明文密钥迁移可成功、失败回滚且不会丢失。
- 隐私说明与实际发送、保存行为一致。

## 6. M10：稳定性与可观测性

### M10 当前执行状态

状态：`completed`

已完成：

- 建立结构化滚动日志、固定上限崩溃摘要和用户主动导出的匿名诊断包。
- Chat Provider 支持连接与模型检查、错误分类恢复建议和失败回复重试。
- 直播房间使用独立指数退避，失败保留最后可信状态，并在托盘展示健康信息。
- 聊天数据库迁移前备份，损坏时可只读导出或确认后备份重建。
- 配置、聊天、角色、伙伴工坊任务和资料支持统一备份与启动前原子恢复。
- 建立 Windows 10 源码/文件夹版性能基线和超过 10% 的退化判定。
- 详细设计、指标和复测命令见 `docs/M10_STABILITY.md`。

完成证据：294 项测试与 32 个子测试、78.07% 分支覆盖率、Ruff、Pyright、
PyInstaller 双形态构建和隔离 EXE 冒烟均通过。

### 6.1 工作项

1. 统一诊断
   - 使用结构化日志事件，增加版本、组件、操作和错误分类字段。
   - 默认日志滚动和体积上限，避免长期运行无限增长。
   - 全局未捕获异常生成本地崩溃摘要，用户确认后才导出。
   - 建立统一匿名诊断包，复用伙伴工坊现有脱敏和哈希校验设计。

2. AI Provider 健康
   - 增加连接测试、模型列表、Tool Calls/流式能力探测。
   - 对超时、认证、权限、限流、模型不存在和协议不兼容给出不同恢复建议。
   - 保存最近成功配置，但不保存密钥。
   - 增加请求取消、重试回答和从失败消息继续功能。

3. 直播监听可靠性
   - 房间级指数退避和随机抖动，避免接口异常时固定频率重试。
   - 区分“未开播”和“检测失败”，失败不得覆盖上次可信状态。
   - 显示每个平台最后成功时间、连续失败次数和当前退避时间。
   - 用录制响应建立平台契约测试，并设置定时健康工作流。

4. 数据可靠性
   - SQLite 启用 schema 版本、迁移事务和启动前备份。
   - 检测数据库损坏并提供只读导出和重建选项。
   - 配置、聊天库、角色目录和孵化任务提供统一备份/恢复入口。

5. 性能基线
   - 记录文件夹版和便携版冷启动、空闲 CPU、内存、动画帧耗时。
   - 记录聊天长会话、44 状态工坊任务和 20 个直播间监听的压力基线。
   - 每个版本与上一个稳定版本比较，关键指标退化超过 10% 时需要说明。

### 6.2 完成门禁

- 网络失败、数据库损坏和未捕获异常都有用户可理解的恢复入口。
- 直播检测失败不会产生错误的“下播”状态。
- 日志和诊断包通过隐私测试并有固定体积上限。
- 关键性能指标有基线，发布候选不存在未解释的显著退化。

## 7. M11：安装、升级与自动更新

### M11 当前执行状态

状态：`completed`

已完成：

- Inno Setup 当前用户安装器、固定 AppId、覆盖升级、卸载数据选择和启动项清理。
- Ed25519 签名清单、HTTPS/字段/大小/SHA-256 验证和原子下载。
- stable/beta 渠道、每天最多一次检查、用户确认和便携版更新隔离。
- 更新前全量数据备份、安装器本地留存与哈希验证回滚入口。
- CI 安装器编译和正式 Release 签名清单阻断门禁。
- 设计与证据见 `docs/M11_INSTALL_UPDATE.md`。

完成证据：310 项测试与 32 个子测试、77.84% 分支覆盖率、三种交付形态
构建、双 EXE 冒烟、安装器独立目录安装/启动/保留数据卸载均通过。

### 7.1 安装器选择

在不购买代码签名证书的前提下，优先采用 Inno Setup：

- 默认按当前用户安装，不要求管理员权限。
- 固定 AppId，支持覆盖升级和标准卸载。
- 安装器提供“创建快捷方式”“开机启动”选项，但默认不强制开启。
- 保留现有文件夹 ZIP 和便携版 EXE，安装器作为推荐交付形态。

MSIX 在未签名场景下部署体验较差，本轮不作为主方案。

### 7.2 自动更新安全模型

代码签名不在本计划范围内，但更新链路仍必须防止产物被替换：

- 更新清单通过 HTTPS 获取。
- 清单使用离线 Ed25519 私钥签名，应用内只嵌入公钥。
- 每个产物记录版本、渠道、大小、SHA-256、下载地址和最低兼容版本。
- 下载完成后先验证清单签名，再验证文件哈希，任何一步失败都拒绝执行。
- 更新器不得接收清单之外的命令、参数或脚本。
- 更新前备份配置和数据库；失败后恢复上一版本。

Ed25519 更新清单签名属于应用自己的更新完整性机制，不替代 Windows 代码签名，
也不会消除 SmartScreen 提示。

### 7.3 工作项

- 编写 Inno Setup 配置和自动构建步骤。
- 定义安装目录、用户数据目录、快捷方式和开机启动清理规则。
- 建立旧版本覆盖升级、降级拒绝、卸载保留/删除用户数据测试。
- 增加 stable、beta 两个更新渠道。
- 更新检查默认每天最多一次，可关闭；下载和安装必须由用户确认。
- 保留最近一个可回滚版本及对应配置/数据库备份。

### 7.4 完成门禁

- 全新安装、覆盖升级、取消升级、失败回滚和卸载均通过自动/人工验收。
- 卸载后无残留开机启动项和运行进程。
- 用户数据的“保留”与“彻底删除”选择行为准确。
- 篡改更新清单、下载文件或哈希时，应用均拒绝更新。
- 便携版不会误用安装版更新路径。

## 8. M12：产品能力增强

### M12 当前执行状态

状态：`completed`

已完成：

- 可控长期记忆：本机候选、逐条确认、用户/角色分区、增删改停用和逐回复使用披露。
- 按住说话、独立语音端点/凭据/隐私确认、持续麦克风状态和四类全局快捷键。
- 只读角色包检查与预览、官方/第三方信任隔离、兼容目录、哈希更新和行为编辑器。
- AI 回答预设、延迟/token/费用估算、停止/重试/编辑重发和 Provider 能力前置阻断。
- 直播适配器插件协议、严格 URL 校验、房间凭据隔离、免打扰、补/重复提醒和分组。
- 详细设计、隐私边界、兼容说明和验收见 `docs/M12_PRODUCT_CAPABILITIES.md`。

完成证据：333 项测试与 32 个子测试、总覆盖率 77.63%、Ruff/Pyright 全绿，
隔离源码、PyInstaller 文件夹版与便携版冒烟通过。

以下五个主题分别建立了独立需求、隐私评估和验收计划。

### 8.1 可控长期记忆

- 从聊天历史中提取候选记忆，必须由用户确认后保存。
- 记忆按角色/用户分区，可查看、编辑、删除和完全关闭。
- 每次回答可查看实际使用了哪些记忆。

### 8.2 语音与快捷操作

- 按键说话优先于常驻麦克风监听。
- 麦克风启用状态必须持续可见。
- 增加全局快捷键：显示/隐藏、打开聊天、停止生成、静音。
- 语音数据的发送端点和保留策略必须单独说明。

### 8.3 角色生态

- 独立角色包检查器和预览器。
- 官方包索引、版本更新和兼容性提示。
- 第三方包只做索引和指纹校验，不默认信任作者声明。
- 动作权重、冷却、贴边露出和预览在可视化编辑器中调整。

### 8.4 AI 体验

- 模型预设、延迟/费用显示、请求取消、重试和编辑后重发。
- 对不同 Provider 显示流式、Tool Calls、图像编辑等能力。
- 不支持的能力在开始任务前阻断，而不是运行中失败。

### 8.5 直播与提醒

- 平台适配器插件协议。
- 提醒静默时段、重复提醒策略和房间分组。
- 平台扩展必须先通过 URL 安全、凭据隔离和契约测试。

## 9. 版本建议

版本号应以实际交付能力为准：

- `1.3.x`：只做缺陷修复、文档同步和发布流程修正。
- `1.4.0`：完成 M7，形成第一个有完整质量门禁的发行版。
- `1.5.0`：完成 M8、M9，形成新的架构和权限基线。
- `1.6.0`：完成 M10、M11，提供稳定安装、升级、诊断和回滚。
- `2.0.0`：仅在存储格式、插件生态或交互模型出现不兼容变更时使用。

## 10. 执行与跟踪规则

每个里程碑开始前创建独立任务清单，任务至少包含：

- 负责人、目标版本、依赖和预计工作量。
- 设计说明、实现提交、自动化测试和人工验收记录。
- 隐私/安全影响和兼容迁移说明。
- 阻断问题、已知限制和回滚方式。

任务状态统一使用：

- `planned`：已进入路线但尚未开始。
- `in_progress`：已开始且有负责人。
- `blocked`：存在明确外部或技术阻断。
- `verification`：实现完成，等待自动/人工验收。
- `done`：验收标准全部满足并进入主分支。

里程碑只有在所有 P0/P1 阻断项为 `done`、发布清单有证据、文档已同步时才能关闭。
