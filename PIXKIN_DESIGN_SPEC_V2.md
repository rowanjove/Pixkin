# Pixkin 角色、动画、贴边、人设与伙伴工坊设计规范 v2

状态：已确认的设计基线  
适用产品：Pixkin 2.x  
目标平台：Windows 10 / 11 x64  
制定日期：2026-07-23

## 1. 规范目的

本规范统一 Pixkin 的角色视觉、动画资产、运行时状态机、贴边交互、人设配置、
角色包格式、伙伴工坊生成流程、质量验收和兼容策略。

后续实现、内置角色重绘、自定义角色孵化、角色包导入和发行验收均以本规范为准。

本文使用以下术语：

- **必须（MUST）**：不满足即不能通过验收。
- **应该（SHOULD）**：默认实现；偏离时必须写明理由。
- **可以（MAY）**：可选能力，不影响基础兼容。

## 2. 已确认的产品决策

### 2.1 首批角色

| 角色 | 分发方式 | v2 质量等级 | 要求 |
|---|---|---:|---|
| 山山 | 软件内置 | 完整级 | 随发行版安装，不可删除 |
| 凛凛 | 软件内置 | 完整级 | 随发行版安装，不可删除 |
| Pip | 软件内置 | 完整级 | 随发行版安装，不可删除 |
| 椰子 | 独立 ZIP | 完整级 | 不自动安装，不进入内置角色列表 |

椰子虽然不随软件内置，但美术、动作、人设和 QA 标准不得低于内置角色。

### 2.2 总体设计决策

- 允许对现有角色进行统一 Q 版重绘，但必须保留角色识别特征。
- 所有新角色必须遵循同一套 Pixkin 视觉规范。
- 运行时支持左、右、上、下四边贴边；底边默认关闭。
- 角色默认只主动做动作，不主动发言、发送消息或打开窗口。
- Windows 10 / 11 是 v2 的唯一正式支持平台。
- v2 必须兼容现有 v1 角色包，但 v1 角色只能获得兼容级体验。

## 3. 产品目标与非目标

### 3.1 产品目标

1. 角色在静止、交互、AI 工作、提醒、移动和贴边状态下都有明确且连贯的表现。
2. 三个内置角色拥有一致的制作质量，同时通过动作节奏表现不同性格。
3. 新角色可依据固定模板稳定生产，不依赖开发者临时判断。
4. 伙伴工坊可中断、可恢复、可单动作返工，生成后先预览再安装。
5. v1 角色包继续可用，但不会被错误标记为完整多帧角色。
6. 所有发布角色均有机器 QA、视觉 QA、动画预览和来源记录。

### 3.2 当前非目标

- v2 不承诺 macOS、Linux、移动端兼容。
- v2 不让角色默认主动聊天。
- v2 不提供骨骼动画、3D 模型或实时布料模拟。
- v2 不允许角色包执行脚本或携带可执行代码。
- v2 不要求第三方基础级角色实现全部移动和四向贴边资源。

## 4. Pixkin 统一视觉规范

### 4.1 核心风格

Pixkin 角色采用“轻像素感 Q 版数字伙伴”风格：

- 紧凑 Q 版比例，头部占总高度约 42%–55%。
- 轮廓清楚、重心稳定，小尺寸下仍能辨认。
- 原生尺寸下使用 1–2 px 深色轮廓，重要外轮廓优先于内部细节。
- 使用有限色板、平涂和最多两级硬边明暗。
- 保留轻微阶梯边缘或像素感，不使用照片级平滑渲染。
- 面部简单、表情明确；眼睛和嘴巴必须在 180 px 显示尺寸下可读。
- 配饰必须简化，不能因细节过密造成动画闪烁。

禁止：

- 写实照片、写实人脸、真实毛发或皮肤材质。
- 3D 渲染、厚涂、动漫立绘、复杂柔光和软渐变。
- 固定地面、场景背景、椭圆阴影、投影和环境光晕。
- 文字、水印、对话框、UI、代码、帧号或可见网格。
- 与角色分离的速度线、运动弧、星星、烟雾、灰尘和标点。

### 4.2 角色身份锁定

每个角色必须有一份“身份基准图”和身份说明，至少固定：

- 头部和身体轮廓。
- 脸型、眼睛、瞳色、嘴部风格。
- 发型、耳朵、角、尾巴等主要结构。
- 主色、辅色和强调色。
- 标志性服装、饰品、道具及所在侧。
- 身体比例、轮廓线粗细和阴影规则。
- 不得变化的角色识别特征。

所有动作必须与身份基准图保持同一角色。仅“画风相似”不算通过。

允许动作需要的局部形变，但不得：

- 改变物种、体型、脸型或主要发型。
- 随机增删饰品、角、尾巴、翅膀或道具。
- 无理由改变饰品所在侧。
- 在不同动作中改变主色或轮廓粗细。

### 4.3 角色差异化

统一风格不等于统一性格。角色差异主要通过以下部分表现：

- 轮廓与标志性元素。
- 表情幅度。
- 动作速度和停顿。
- 动作出现概率。
- 姿态开放或克制程度。
- 对提醒、成功和失败的反应。

不得通过破坏画布、帧率或轮廓规范来制造差异。

## 5. 动画画布与资产标准

### 5.1 标准单元格

所有 v2 动画最终必须归一化为：

- 单帧尺寸：`192 × 208 px`
- 色彩空间：sRGB
- 像素格式：RGBA
- 中心水平线：`x = 96`
- 地面基准线：`y = 194`
- 左右安全边距：各 12 px
- 顶部安全边距：10 px
- 底部安全边距：8 px
- 默认枢轴：`(96, 194)`

站立、待机和手势动作的接地点应该位于 `y = 194 ± 2 px`。
跳跃和贴边动作可以离开地面基准线，但水平角色中心仍应稳定在 `x = 96 ± 3 px`。

### 5.2 帧稳定性

- 同一动作内，非动作需要导致的身体尺度变化不得超过 3%。
- 相邻站立动作切换时，头部尺度变化不得超过 4%。
- 手势、头发、尾巴和道具不得越过单元格安全区。
- 角色任何像素不得进入相邻帧。
- 未使用帧必须完全透明。
- 半透明像素只允许用于必要的边缘抗锯齿，不得形成软阴影或光晕。

### 5.3 推荐图集

首选图集页面固定为：

- 8 列 × 9 行
- 单格 `192 × 208`
- 页面尺寸 `1536 × 1872`
- PNG 或无损 WebP

由于 v2 动作多于 9 行，一个角色可以拥有多个图集页面：

```text
images/
├─ atlas-core.webp
├─ atlas-expression.webp
├─ atlas-movement.webp
└─ atlas-edge.webp
```

`atlas-core.webp` 应优先使用标准九行动作布局，以便复用现有孵化与 QA 工具。
其他页面由 `character.md` 中的行、列和帧数明确声明。

角色包作者也可以提交独立帧或动作条。安装器必须把所有形式归一化成同一内部帧模型，
运行时不得为三种来源分别维护不同的状态机。

## 6. 动作等级

### 6.1 基础级

自定义 v2 角色包最低可导入等级：

| 动作 ID | 中文语义 | 最低帧数 |
|---|---|---:|
| `idle` | 基础待机 | 4 |
| `talking` | 正在说话 | 4 |
| `dragging` | 被拖拽 | 3 |
| `alerting` | 普通提醒 | 4 |

缺少其他动作时，运行时可以回退到 `idle`，但设置页必须显示“基础级”，不能显示为完整角色。

### 6.2 标准级

标准级必须包含基础级，并增加：

| 动作 ID | 中文语义 | 建议帧数 | FPS | 循环 |
|---|---|---:|---:|---|
| `blink` | 眨眼 | 3–4 | 10 | 否 |
| `look_around` | 张望 | 4–6 | 8 | 否 |
| `stretch` | 伸懒腰 | 5–6 | 8 | 否 |
| `wave` | 挥手 | 4–6 | 8 | 否 |
| `nod` | 点头 | 4 | 8 | 否 |
| `sleep` | 睡眠循环 | 6 | 6 | 是 |
| `wake` | 醒来 | 4–6 | 8 | 否 |
| `listening` | 倾听用户 | 4 | 8 | 是 |
| `thinking` | 等待模型 | 6 | 8 | 是 |
| `working` | 调用工具/处理任务 | 6 | 8 | 是 |
| `waiting` | 普通等待 | 6 | 6 | 是 |
| `success` | 完成任务 | 5–6 | 10 | 否 |
| `failed` | 失败或请求出错 | 6–8 | 8 | 否 |
| `touch` | 被点击/摸头 | 4–6 | 10 | 否 |
| `happy` | 开心 | 5–6 | 10 | 否 |

### 6.3 完整级

完整级必须包含基础级和标准级，并增加：

| 动作 ID | 中文语义 | 建议帧数 | FPS | 循环 |
|---|---|---:|---:|---|
| `annoyed` | 轻微嫌弃/被连续打扰 | 4–6 | 8 | 否 |
| `walk_left` | 向左移动 | 6–8 | 10 | 是 |
| `walk_right` | 向右移动 | 6–8 | 10 | 是 |
| `run_left` | 向左跑 | 8 | 12 | 是 |
| `run_right` | 向右跑 | 8 | 12 | 是 |
| `jump` | 跳跃 | 5–6 | 10 | 否 |
| `land` | 落地恢复 | 4 | 10 | 否 |
| `alerting_important` | 重要提醒 | 6–8 | 10 | 否 |
| `celebrate_live` | 开播庆祝 | 6–8 | 10 | 否 |

完整级还必须提供第 8 节定义的四向贴边动作。

山山、凛凛、Pip 和独立 ZIP 椰子均按完整级制作。

## 7. 动画播放与状态机

### 7.1 运行时阶段

每个动作可以声明：

- `enter`：进入动作。
- `loop`：可循环主体。
- `exit`：退出动作。
- `once`：只播放一次的完整动作。

简单动作可以只有 `once` 或 `loop`。复杂上下文动作应该采用
`enter → loop → exit`，避免突然切换。

### 7.2 优先级

| 优先级 | 类别 | 例子 |
|---:|---|---|
| 100 | 强制用户交互 | `dragging` |
| 90 | 重要提醒 | `alerting_important`、`celebrate_live` |
| 80 | 直接触摸交互 | `touch`、`annoyed` |
| 70 | AI 前台状态 | `listening`、`thinking`、`talking`、`working` |
| 60 | 移动与贴边过渡 | 行走、跑步、贴边进入/退出 |
| 40 | 主动手势 | `wave`、`stretch`、`nod` |
| 10 | 环境动作 | `idle`、`blink`、`look_around`、`sleep` |

高优先级动作可以打断低优先级动作。低优先级动作不得打断高优先级动作。

`dragging` 可以立即打断任何状态。重要提醒不得打断拖拽。

### 7.3 状态恢复

状态机必须维护上下文栈，而不是所有动作结束后都回到 `idle`。

例：

```text
edge_idle_left
→ edge_exit_left
→ alerting_important
→ edge_enter_left
→ edge_idle_left
```

如果提醒前角色处于贴边状态，提醒结束后必须回到原显示器、原边和原贴边位置，
除非用户在提醒期间主动拖动或关闭贴边。

### 7.4 AI 生命周期映射

| 产品事件 | 动画状态 |
|---|---|
| 用户发送消息 | `listening`，持续 300–700 ms |
| 等待模型首个输出 | `thinking` |
| 模型流式输出 | `talking` |
| 执行 Tool Call | `working` |
| 回复成功完成 | `success`，随后恢复上下文 |
| API 或工具失败 | `failed`，随后恢复上下文 |
| 普通系统提醒 | `alerting` |
| 重要提醒 | `alerting_important` |
| 主播开播 | `celebrate_live` |

角色动画只能表达当前状态，不得因为动画状态自行发送文字。

### 7.5 随机行为调度

随机行为必须由每个角色的 `behavior` 配置控制：

- 空闲动作间隔范围。
- 每个动作权重。
- 动作冷却时间。
- 连续动作组合。
- 睡眠触发时间。
- 动作速度倍率。
- 动作幅度等级。

运行时必须满足：

- 同一非眨眼动作不得连续出现两次。
- 最近三个主动动作内，应该避免完全重复。
- 用户正在输入、模型正在回复、全屏程序运行或系统处于省电模式时，降低环境动作频率。
- “减少动态”开启后，禁止跳跃、快速晃动和高频环境动作。

## 8. 四向贴边规范

### 8.1 支持范围

运行时必须支持：

- 左边贴边。
- 右边贴边。
- 上边贴边。
- 下边贴边。

默认配置：

```yaml
edge_sides:
  left: true
  right: true
  top: true
  bottom: false
```

底边关闭是默认值，但不是功能缺失。用户可以在设置中手动开启。

### 8.2 每个方向的动作阶段

每个方向包含四个状态：

```text
edge_enter_<side>
edge_idle_<side>
edge_hover_<side>
edge_exit_<side>
```

完整集合：

```text
edge_enter_left      edge_idle_left      edge_hover_left      edge_exit_left
edge_enter_right     edge_idle_right     edge_hover_right     edge_exit_right
edge_enter_top       edge_idle_top       edge_hover_top       edge_exit_top
edge_enter_bottom    edge_idle_bottom    edge_hover_bottom    edge_exit_bottom
```

建议帧数：

- `edge_enter_*`：4–6 帧，非循环。
- `edge_idle_*`：4–6 帧，循环。
- `edge_hover_*`：4–6 帧，循环。
- `edge_exit_*`：4–6 帧，非循环。

### 8.3 专用构图与镜像

- 四个方向必须有独立的可见区域锚点。
- 左右动作只有在角色完全对称时才可以镜像。
- 存在单侧饰品、文字、徽标、道具、发束或不对称光照时不得镜像。
- 上下动作不得通过简单旋转左右动作获得。
- 贴边时应优先露出脸、眼睛和标志性饰品，而不是缩小整只角色。

### 8.4 吸附行为

- 吸附距离：默认 24 个逻辑像素。
- 脱离迟滞：默认 40 个逻辑像素。
- 所有距离以 Qt 逻辑像素计算，不直接使用物理像素。
- 角色释放在角落区域时，根据最近边和最后拖动方向选择目标边。
- 距离完全相同时优先选择左右边，避免意外进入顶部或任务栏区域。
- 禁用的边不得参与候选计算。
- 吸附过程使用 240–320 ms 的缓出动画。

### 8.5 露出尺寸

贴边露出使用比例而不是固定缩图：

- 默认露出角色窗口短边的 25%。
- 最小 36 个逻辑像素。
- 最大 64 个逻辑像素。
- 用户设置值必须在缩放后再次钳制。

每个方向可以在角色包中声明 `peek_anchor` 和 `hitbox`。
可点击区域不得小于 `36 × 36` 逻辑像素。

### 8.6 悬停和点击

- 鼠标进入贴边可见区域 180 ms 后播放 `edge_hover_*`。
- 悬停时角色可以再探出 15%–25%，并看向鼠标。
- 鼠标离开后平滑回到 `edge_idle_*`。
- 单击时完整退出贴边，并打开聊天窗口。
- 按住拖动时立即进入 `dragging`，不得先打开聊天。
- 双击可以切换“固定在当前边”，但不得改变默认单击行为。

### 8.7 贴边环境动作

角色贴边超过一定时间后，可以低频播放：

- 眨眼。
- 偷看。
- 缩回。
- 打盹。

贴边动作不得主动发言、弹窗或抢占键盘焦点。

### 8.8 多显示器与任务栏

- 使用角色中心或鼠标释放点所在显示器的 `availableGeometry`。
- 记录显示器标识、贴边方向和沿边归一化位置。
- 显示器断开后，将角色迁移到主屏幕最近的安全位置。
- DPI 变化后重新计算窗口和露出尺寸，不得沿用旧物理像素。
- 底边贴边必须尊重任务栏实际位置和工作区，不能覆盖任务栏交互区域。

## 9. 角色人设规范

### 9.1 结构化人设

v2 角色必须使用结构化人设，至少包含：

```yaml
persona:
  identity: "角色是谁"
  background: "简短背景和世界观"
  core_traits: ["核心性格 1", "核心性格 2"]
  contrast_trait: "让角色不扁平的矛盾点"
  relationship: "与用户的关系"
  user_address: "对用户的默认称呼"
  voice:
    tone: "语气"
    pacing: "节奏"
    reply_length: "short"
    vocabulary: "用词偏好"
  catchphrases:
    - text: "口癖"
      max_frequency: 0.08
  likes: []
  dislikes: []
  emotion_rules: []
  initiative:
    animate_without_prompt: true
    speak_without_prompt: false
    open_windows_without_prompt: false
  tool_behavior:
    before_call: "调用工具前如何说明"
    after_success: "成功后如何说明"
    after_failure: "失败后如何说明"
  boundaries: []
  examples:
    - user: "示例用户消息"
      assistant: "符合角色口吻的示例回复"
```

### 9.2 主动行为边界

默认必须为：

```yaml
initiative:
  animate_without_prompt: true
  speak_without_prompt: false
  open_windows_without_prompt: false
  send_notifications_without_prompt: false
```

角色可以主动做动作，但不得主动：

- 在聊天中发送文字。
- 调用 AI 接口或本地工具。
- 打开窗口。
- 修改配置。
- 发送系统通知。

未来若提供主动发言功能，必须由用户显式开启，并与角色包默认值分离。

### 9.3 人设与动作连接

人设必须包含动作气质配置：

```yaml
behavior:
  motion_temperament: calm
  speed_multiplier: 0.9
  amplitude: low
  idle_interval_seconds: [9, 18]
  ambient_weights:
    blink: 5
    look_around: 3
    stretch: 1
    wave: 0.5
  cooldown_seconds:
    wave: 45
    stretch: 60
```

首批角色方向：

| 角色 | 动作气质 | 主要表现 |
|---|---|---|
| 山山 | 温柔、沉静 | 点头、倾听、阅读感、柔和停顿 |
| 凛凛 | 克制、可靠 | 张望、思考、动作幅度小、反应利落 |
| Pip | 活泼、淘气 | 挥手、跳跃、反应快、动作幅度较大 |
| 椰子 | 慢热、柔和 | 打盹、缓慢挥手、舒展、低频动作 |

### 9.4 人设覆盖层

人设必须分为三层：

```text
角色包原始人设
→ 用户对该角色的持久覆盖
→ 当前会话临时状态
```

- 原始人设随角色包安装，不能被设置页直接覆盖。
- 用户覆盖按角色 ID 独立保存。
- 切换角色时恢复该角色自己的覆盖，而不是使用全局公共提示词。
- 设置页必须支持查看差异和恢复角色默认。
- 重命名只修改显示名称或明确的人设字段，禁止对整段提示词做字符串全局替换。

## 10. 角色包规范 v2

### 10.1 目录结构

推荐完整包：

```text
my-character.zip
├─ character.md
├─ images/
│  ├─ preview.png
│  ├─ atlas-core.webp
│  ├─ atlas-expression.webp
│  ├─ atlas-movement.webp
│  └─ atlas-edge.webp
├─ references/
│  └─ identity.png
└─ qa/
   └─ validation.json
```

发行包可以省略生成过程文件和 QA 视频，但构建源必须保留接触表、预览视频和审核记录。

### 10.2 顶层字段

`character.md` 必须包含：

```yaml
schema_version: "2.0"
id: my-character
name: 我的角色
version: 2.0.0
author: 作者
description: 简短介绍
quality_tier: full
preview: images/preview.png
persona: {}
behavior: {}
animations: {}
compatibility: {}
rights: {}
```

字段要求：

- `schema_version`：必须为受支持的语义版本。
- `id`：2–48 位小写字母、数字、下划线或短横线。
- `name`：1–32 个显示字符。
- `version`：语义版本。
- `quality_tier`：`basic`、`standard` 或 `full`。
- `persona`：第 9 节结构化人设。
- `behavior`：角色动作调度配置。
- `animations`：动作资源与播放信息。
- `rights`：版权、来源和 AI 生成声明。

未知字段只能放在 `extensions` 命名空间中。顶层拼写错误必须报错，不能静默忽略。

### 10.3 动画声明

图集动作示例：

```yaml
animations:
  idle:
    source:
      type: atlas
      file: images/atlas-core.webp
      row: 0
      column: 0
      frames: 6
      cell_size: [192, 208]
    fps: 8
    playback: loop
    anchor: [96, 194]
    priority: 10
    interruptible: true
```

动作条示例：

```yaml
animations:
  wave:
    source:
      type: strip
      file: images/wave-strip.png
      frames: 5
      direction: horizontal
      cell_size: [192, 208]
    fps: 8
    playback: once
```

独立帧示例：

```yaml
animations:
  blink:
    source:
      type: frames
      files:
        - images/blink-01.png
        - images/blink-02.png
        - images/blink-03.png
    fps: 10
    playback: once
```

`playback` 允许：

- `once`
- `loop`
- `ping_pong`

### 10.4 贴边元数据

```yaml
edge:
  supported_sides: [left, right, top, bottom]
  default_enabled_sides: [left, right, top]
  left:
    peek_anchor: [164, 98]
    hitbox: [132, 56, 60, 84]
  right:
    peek_anchor: [28, 98]
    hitbox: [0, 56, 60, 84]
  top:
    peek_anchor: [96, 172]
    hitbox: [54, 144, 84, 64]
  bottom:
    peek_anchor: [96, 34]
    hitbox: [54, 0, 84, 64]
```

坐标均基于 `192 × 208` 单元格。

### 10.5 权利与来源

```yaml
rights:
  license: "CC-BY-4.0 / proprietary / personal-use"
  author_confirmed_rights: true
  ai_generated: true
  reference_sources:
    - type: user_provided
      description: "用户提供的角色参考图"
  modification_allowed: true
  commercial_use_allowed: false
```

导入器不负责法律判断，但必须显示包作者声明。缺少
`author_confirmed_rights: true` 的第三方包应显示警告。

### 10.6 安全限制

- 解压总大小不超过 100 MB。
- 推荐发行包不超过 60 MB。
- 单文件不超过 25 MB。
- 文件数量不超过 500。
- 单张图片像素总量不超过 20 MP。
- 禁止绝对路径、路径穿越和符号链接。
- 禁止脚本、动态库和可执行文件。
- 只允许 PNG、无损 WebP、JPG/JPEG 预览图和声明的文本/JSON 文件。

## 11. 伙伴工坊 v2

### 11.1 生成模式

| 模式 | 用途 | 典型生成任务 |
|---|---|---:|
| 基础孵化 | 确认角色风格并获得可安装伙伴 | 1 张身份基准图 + 4 个基础状态（5 次图像生成） |
| 标准孵化 | 普通自定义角色 | 1 张身份基准图 + 19 个标准状态（20 次图像生成） |
| 完整孵化 | 完整移动与贴边 | 标准孵化 + 移动 + 四向贴边 |

界面必须在开始前显示预计任务数以及 API 成本、耗时由接口、模型和质量决定的提示。
只有规定状态全部齐全时，生成器才能声明相应质量等级；历史 9 动作任务恢复完成后
仍应声明为基础级，不得静默升级为标准级。

### 11.2 强制流程

```text
输入角色信息与参考图
→ 生成身份基准图
→ 用户确认身份
→ 生成 idle 与 run_right
→ 用户确认一致性和步态
→ 生成剩余动作
→ 自动 QA
→ 接触表与视频预览
→ 用户确认
→ 安装并可选设为当前角色
```

不得在没有最终预览确认的情况下自动覆盖同 ID 角色。

### 11.3 生成约束

- 每个动作条都必须使用已确认的身份基准图。
- 原始参考图只作为身份辅助，不得替代基准图。
- 先完成 `run_right`，再判断 `run_left` 是否可安全镜像。
- 只要有单侧配饰、文字、徽标、道具或不对称设计，就必须单独生成 `run_left`。
- 背景色键必须按角色色板自动选择，不能固定使用绿色。
- 动作条必须使用对应布局参考，但输出不得出现参考框、网格和标签。
- 失败时只重做最小动作行，不得自动重跑整个角色。

### 11.4 任务持久化

伙伴工坊必须支持：

- 每个生成任务独立记录状态。
- 退出程序后继续未完成任务。
- 单动作重试。
- 保留原始生成结果及来源记录。
- 用户选择候选版本。
- 取消后不把未完成包安装为角色。

## 12. QA 与发布门槛

### 12.1 自动检查

每个 v2 包必须自动检查：

- YAML/Schema 可解析。
- 质量等级对应的动作齐全。
- 所有图片可解码。
- 图片尺寸、单元格和帧数正确。
- RGBA 与透明区符合要求。
- 未使用图集单元格完全透明。
- 角色没有碰到安全区边缘。
- 不同语义动作不是完全相同文件。
- 精确重复哈希只有在显式 `alias_of` 时允许。
- FPS、播放模式、优先级和锚点在允许范围。
- 资源路径安全。
- 包大小、文件数量和像素数量在限制内。

### 12.2 视觉检查

必须生成：

- 全动作接触表。
- 每个动作的循环预览视频或 GIF。
- 四向贴边交互预览。
- 角色身份对比图。
- 自动验证报告。

视觉审核必须拒绝：

- 角色身份漂移。
- 白底、黑底、色键残留或明显绿边。
- 裁切、跨格、重复姿势或空帧。
- 脚底无理由跳动。
- 动作中角色尺寸漂移。
- 禁止的阴影、光晕、速度线和游离特效。
- 不完整循环或动作语义不清。
- 左右镜像导致饰品、文字或道具方向错误。

### 12.3 发布标准

内置角色和椰子 ZIP 发布时必须：

- 自动 QA 为 0 个错误。
- 没有未处理警告。
- 人工逐行确认身份一致性。
- 四向贴边均完成测试。
- 接触表和预览已归档。
- `character.md`、图集和 QA 报告版本一致。

## 13. 设置与管理界面

### 13.1 角色管理

角色列表必须显示：

- 名称和预览。
- 内置/自定义/独立 ZIP 标识。
- 基础级/标准级/完整级。
- v1 兼容角色标识。
- QA 状态。
- 版本、作者和许可证。

角色详情页应支持逐动作预览。

### 13.2 人设编辑

提供两种模式：

- 简单模式：性格预设、回复长度、语气、主动动作频率和动作幅度。
- 进阶模式：结构化人设字段和最终系统提示词预览。

必须提供：

- 查看与角色默认值的差异。
- 恢复角色默认。
- 单独恢复人设或动作偏好。

### 13.3 桌面行为

设置项至少包含：

- 左、右、上、下边独立开关。
- 底边默认关闭。
- 贴边露出比例。
- 悬停探出开关。
- 通知后返回贴边开关，默认开启。
- 减少动态。
- 省电模式。
- 全屏时安静。
- 环境动作频率。

## 14. v1 兼容与迁移

### 14.1 v1 行为

- v1 包继续允许导入。
- v1 单帧动作继续使用程序化轻微呼吸、点头和说话变形。
- v1 不得被标记为标准级或完整级。
- 设置页显示“v1 兼容角色”。
- 缺失动作回退到 `idle`。

### 14.2 状态别名

迁移器至少支持：

```yaml
aliases:
  waving: wave
  jumping: jump
  review: working
  running-left: run_left
  running-right: run_right
```

旧版无方向的 `running` 必须根据实际移动方向动态映射到 `run_left` 或
`run_right`。旧版 `edge_docked` 必须根据当前贴边方向动态映射到对应的
`edge_idle_*`，并在四个方向复用同一旧资源。它只能作为兼容回退，不能自动宣称
拥有完整四向贴边。

### 14.3 迁移工具

迁移工具应该：

1. 检测重复图片和单帧伪动作。
2. 检测透明度、尺寸和裁切。
3. 生成 v2 草稿清单。
4. 标出缺失动作。
5. 保留原角色包，不原地覆盖。
6. 允许用户逐步补齐到标准级或完整级。

## 15. 性能与可访问性

### 15.1 性能目标

- 默认动画刷新上限：25–30 FPS。
- 空闲状态 CPU 目标：常见桌面设备上低于 2%。
- 单角色解码后内存建议：低于 80 MB。
- 图集按页懒加载，冷门贴边页可延迟解码。
- 动作切换不得同步读取大量磁盘文件。
- 支持 100%–250% DPI。
- 多显示器切换后角色位置和贴边状态保持有效。

### 15.2 减少动态

开启“减少动态”时：

- 禁止连续跳跃和快速旋转。
- 环境动作间隔至少翻倍。
- 跑步可回退为慢速行走或平滑位移。
- 贴边悬停不使用快速弹性效果。
- 重要提醒仍然可见，但降低幅度。

### 15.3 省电与全屏

- 电池供电时可以降低到 15 FPS。
- 全屏应用运行时暂停非必要环境动作。
- 省电和全屏策略不得暂停正在进行的拖拽、聊天状态或重要提醒。

## 16. 测试要求

### 16.1 单元测试

必须覆盖：

- 状态优先级和可打断规则。
- 状态上下文恢复。
- 随机动作权重、冷却和去重。
- FPS、循环和非循环动作播放。
- 三种资源来源的统一解析。
- Schema v2 验证。
- v1 回退与状态别名。
- 用户人设覆盖分层。

### 16.2 集成测试

必须覆盖：

- AI 生命周期到动画状态映射。
- Tool Call 成功和失败。
- 四方向贴边进入、悬停、退出。
- 通知离开贴边并返回。
- 四个屏幕边角。
- 多显示器和显示器拔插。
- 100%、150%、200%、250% DPI。
- 顶部/侧边/底部任务栏。
- 角色切换和图集资源释放。

### 16.3 视觉回归

每个内置角色必须保存基准截图或接触表，用于检测：

- 背景矩形。
- 角色尺寸变化。
- 脚底漂移。
- 贴边可见区域错误。
- 图集错行、错列或透明区错误。

视觉回归不能替代人工动画审查。

## 17. 实施里程碑

### M0：规范冻结

- 确认本规范。
- 建立角色身份卡模板、动作清单和 QA 模板。

### M1：v2 运行时与角色包

- 实现 Schema v2。
- 实现图集、动作条和独立帧统一解析。
- 实现状态优先级、上下文恢复和角色行为配置。
- 保持 v1 兼容。

### M2：四向贴边系统

- 实现四方向状态和元数据。
- 实现悬停、点击、拖出和通知返回。
- 完成 DPI、多屏和任务栏测试。

### M3：首批角色视觉基准

- 完成山山、凛凛、Pip 身份基准图。
- 建立统一色板、轮廓和表情规范。
- 用户确认身份后再生成完整动作。

### M4：首批完整角色

- 完成三个内置角色全部动作和四向贴边。
- 完成接触表、视频预览和 QA。
- 替换现有单帧伪动作资源。

### M5：人设与设置中心

- 实现结构化人设。
- 实现每角色独立覆盖、差异查看和恢复默认。
- 加入贴边侧边开关、减少动态和省电设置。

### M6：伙伴工坊 v2

- 身份稿确认。
- 分阶段生成。
- 断点续作和单动作返工。
- 自动 QA、预览和确认后安装。

### M7：椰子独立 ZIP 与发行验收

- 按完整级制作椰子。
- 只作为独立 ZIP 发布。
- 确认启动流程不会自动安装椰子。
- 完成全套回归与发行包校验。

## 18. v2 完成定义

只有同时满足以下条件，Pixkin 动画与角色系统 v2 才算完成：

1. 山山、凛凛、Pip 均达到完整级。
2. 椰子以完整级独立 ZIP 交付，且不随软件自动安装。
3. 所有首批角色遵循统一视觉规范并保留身份特征。
4. 四边贴边可用，底边默认关闭。
5. 通知结束能恢复原贴边上下文。
6. AI 生命周期与动画状态正确联动。
7. 角色默认只主动做动作，不主动发言。
8. v1 角色包仍可安全使用。
9. v2 包通过自动 QA 和人工视觉 QA。
10. 正确环境下所有单元、集成和视觉回归测试通过。
