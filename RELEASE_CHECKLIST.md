# Pixkin 发布前验收清单

本清单适用于 Windows 文件夹版、便携版和随发行附带的角色包。发布负责人应在同一个
候选提交上完成所有必选项；任何阻断项失败都必须重新构建，不能沿用旧产物。

## 1. 冻结发布候选

- [ ] 工作区只包含本次发布需要的改动，已记录候选提交 SHA。
- [x] `core/version.py` 中的 `VERSION` 已按语义化版本更新。
- [x] 已运行 `py -3.11 scripts\generate_version_info.py`。
- [x] 版本资源、README 发行文件名和发布说明使用同一版本号。
- [x] Git LFS 文件不是指针文本，内置角色和示例角色包可正常读取。

## 2. 自动化质量门槛

在仓库根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run_quality.ps1
```

- [ ] 锁定依赖可使用 `--require-hashes` 从干净环境安装。
- [x] Ruff 零错误，Pyright 零错误。
- [x] 全量测试零失败、零意外跳过。
- [x] 项目分支覆盖率不低于 70%，核心模块重点覆盖率没有无说明下降。
- [ ] 版本资源、构建资产、角色包、静态 QA、动画 QA 和伙伴工坊测试全部通过。
- [ ] 基础、标准、完整三档计划量分别保持为 5、20、45；启用安全镜像时完整档为 39。
- [ ] 匿名问题包的白名单、哈希篡改、越权文件和脱敏测试通过。
- [x] `pip-audit` 未发现未处置漏洞，SBOM 已成功生成。
- [ ] Windows Server 2022/2025、100%/200% DPI 自动兼容矩阵全部通过。

## 3. 隐私与安全

- [ ] 仓库和发布目录中没有 API Key、访问令牌、私人参考图或真实用户任务目录。
- [ ] 从伙伴工坊导出一个问题包，再用“检查问题包”验证通过。
- [ ] 问题包只含清单、脱敏诊断、最小运行摘要和允许的 QA JSON 摘要。
- [ ] 清单隐私声明为全否，所有非清单文件的大小与 SHA-256 均验证通过。
- [ ] 复制的 GitHub Issue 模板不含运行 ID、角色名字、自由文本、原始错误或本地路径。
- [ ] 对话和图像 API Key 在配置、日志、聊天数据库、工具审计和问题包中均未明文出现。
- [ ] L1 工具拒绝、逐次允许和本次运行记住均按预期；L2/L3 未经授权不能执行。
- [ ] 工具审计可以查看、导出和清除，持久文件不超过 1000 条/1 MiB。
- [ ] 官方角色包 SHA-256 清单随 EXE 打包，篡改 ZIP 会被拒绝。
- [ ] 不保存、7 天、30 天和永久四档聊天保留策略均与说明一致。
- [ ] 长期记忆候选不会自动保存；总关闭后请求不含记忆，回复披露与实际注入一致。
- [ ] 语音录音不落盘，独立端点/凭据和首次隐私确认与实际请求一致。
- [ ] 角色目录下载拒绝重定向、超限、哈希篡改、身份错配和未确认安装。
- [ ] 直播配置中的明文 cookie 不会被 Provider 消费，平台/房间凭据互不串用。

## 4. 构建

```powershell
py -3.11 -m pip install --require-hashes -r requirements-lock.txt
powershell -ExecutionPolicy Bypass -File scripts\build_release.ps1
```

- [x] 构建脚本成功退出，`release/` 只包含本版本计划发布的文件。
- [x] `Pixkin-<version>-win64.zip`、`Pixkin-Portable-<version>.exe`、`椰子.zip`、
  `CHARACTER_PACKAGE_SPEC.md`、`SBOM.cdx.json` 和 `SHA256SUMS.txt` 均存在。
- [x] 在重新生成 `SHA256SUMS.txt` 后没有再修改任何发布文件。
- [x] 手工复算至少文件夹版 ZIP、便携版 EXE 和角色包的 SHA-256，结果一致。
- [x] Inno Setup 安装器使用固定 AppId、当前用户目录，覆盖升级没有改变用户数据目录。
- [x] stable 更新清单的 Ed25519 签名、安装器大小和 SHA-256 均使用成品公钥复验通过。
- [ ] 正常安装拒绝降级；受控回滚入口只启动哈希验证通过的上一安装器。
- [ ] 卸载“保留数据”和“彻底删除数据”两条路径均按选择执行，开机启动项均被清理。
- [ ] 使用当前候选运行 `scripts\measure_performance.py` 并与同类 Windows
  基线比较；超过 10% 的退化已修复或在发布说明中解释。
- [ ] 匿名诊断包严格检查通过，且不包含密钥、聊天正文、用户名或本地路径。
- [ ] 全量本地数据备份可以校验、暂存并在下次启动前恢复；失败注入能回滚旧数据。

## 5. 干净 Windows 冒烟

- [ ] Windows 10 与 Windows 11 实机/虚拟机均完成阻断场景验证。
- [ ] 至少验证 125% 和 150% DPI；负坐标副屏、主屏左右切换与任务栏位置变化正常。
- [ ] 显示器热插拔、休眠唤醒后桌宠和聊天窗口仍位于可见区域。
- [ ] 在未安装开发依赖的 Windows 机器或全新虚拟机测试。
- [ ] 文件夹版可启动、托盘可用、退出后无遗留进程。
- [ ] 便携版可启动，并已记录首次启动速度与可能出现的 SmartScreen 提示。
- [ ] 默认角色可显示，透明背景、拖动、行走、贴边和对话气泡正常。
- [ ] 设置保存、开机启动开关和 Windows Credential Manager 密钥保存行为正常。
- [ ] 按住说话、松开停止、麦克风静音、无设备提示和独立转写在真实硬件上正常。
- [ ] 四类全局快捷键可注册、冲突会提示，退出后可被其他程序重新注册。
- [ ] 免打扰跨午夜、静默结束补提醒、重复提醒间隔和房间分组行为正常。
- [ ] 可导入随发行附带的 `椰子.zip`，预览、动画和卸载流程正常。
- [ ] 使用模拟或受控低成本接口走通一次基础孵化：预检、身份审核、QA、最终安装均正常。
- [ ] 中断任务后可恢复；失败动作可单独重试；预算耗尽前会安全停止。

## 6. 发布与回滚

- [x] 发布说明列出主要变化、已知限制、兼容性变化和隐私边界。
- [x] 明确说明二进制当前是否签名，以及 SmartScreen 的预期表现。
- [ ] 上传后重新下载所有资产并按 `SHA256SUMS.txt` 验证。
- [ ] Git 标签指向已验收的候选提交，标签版本与应用版本一致。
- [ ] 保留上一稳定版本的下载链接、校验值和回滚说明。
- [ ] 发布后用公开下载资产再完成一次启动与角色包导入冒烟。

发布签字：

```text
版本：
候选提交：
构建环境：
验收人：
验收时间：
已知限制：
回滚版本：
```
